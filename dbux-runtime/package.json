{
  "private": true,
  "name": "dbux-runtime",
  "version": "1.0.0",
  "description": "",
  "module": "dist/index.js",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "_before-run": "npx link-module-alias",
    "build": "npm run clean && npm run _before-run && webpack --config config/webpack.config.js",
    "test": "node --stack-trace-limit=1000 node_modules/.bin/jest",
    "__postinstall": "npm run add-links",
    "add-links": "npm i --ignore-scripts -S ../dbux-common"
  },
  "devDependencies": {},
  "keywords": [
    "dbux",
    "interactive",
    "code",
    "dynamic",
    "analysis",
    "tracing",
    "babel-plugin"
  ],
  "dependencies": {
    "socket.io-client": "^2.3.0"
  },
  "peerDependencies": {
    "dbux-common": "../dbux-common",
    "lodash": "*",
    "nanoevents": "*"
  },
  "_moduleAliases": {
    "dbux-common": "../dbux-common"
  }
}
