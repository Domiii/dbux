import BaseNode from './BaseNode';

/**
 * TODO: needs its own `traceOE` call, in order to assign children `dataPath`
 *      which requires parent `refId`.
 */
export default class ObjectExpression extends BaseNode {

}