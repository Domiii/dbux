## Promise Roles

1. Participating in `async` function
   * return value of `async` function
   * `awaitArgument`
     * first await
     * not first await
2. other
   * -> requires tracking of promise tree

