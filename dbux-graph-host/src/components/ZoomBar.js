import HostComponentEndpoint from '../componentLib/HostComponentEndpoint';

export default class ZoomBar extends HostComponentEndpoint {
  // this component does nothing on the host side,
  
  // next todo: set zoom origin point with focus or highlight node bounds 
  // get focus() {
  //   return this.context.graphDocument.controller.getComponents('FocusController');
  // }
  // init() {
  //   this.state.focusBounds = {
      
  //   };
  // }
  // getFocus() {
  //   this.setState({
  //     focus: this.focus.state.focus
  //   });
  // }
}