import HostComponentEndpoint from '../../componentLib/HostComponentEndpoint';

export default class PopperController extends HostComponentEndpoint {
  // this controller does nothing on the host side,
  // it helps client side rendering `poppers`
}