import HostComponentEndpoint from '../../componentLib/HostComponentEndpoint';

export default class PopperManager extends HostComponentEndpoint {
  // this manager does nothing on the host side,
  // it helps client side rendering `poppers`
}