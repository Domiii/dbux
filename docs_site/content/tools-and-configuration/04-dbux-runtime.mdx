# Dbux Runtime

import ToolLink from '@src/components/ToolLink';
import CodeLink from '@src/components/CodeLink';
import TOC from '@src/components/TOC';

<TOC toc={toc} />

<CodeLink path="dbux-runtime" /> is injected by the <ToolLink name="dbux-babel-plugin" /> in order to record a JavaScript application's runtime trace log.

Once running, it is responsible for collecting and sending runtime data to the [runtime server](../tools-and-configuration/dbux-code#runtime-server).


## Config

TODO: extract a pretty table of configuration options here.

