/**
 * An external stopwatch that supports `start`, `stop`, `set`, `show`, `hide`
 */
export default class Stopwatch {
  /**
   * Set time to value in ms
   * @param {number} value
   */
  set(value) { }

  start() { }

  stop() { }

  show() { }

  hide() { }
}